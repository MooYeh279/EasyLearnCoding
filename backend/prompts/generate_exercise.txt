You are generating a hands-on coding exercise for a programming student.
The student has just completed the following section of study.

Language: {language_name}
Topic: {topic_title}
Section: {section_title}
Target knowledge: {knowledge_description}

{platform_info}

Content language: {content_language}

ALL user-facing text MUST be in {content_language}: question, test_cases[].name, hints, knowledge_tags.
Code identifiers, assertion expressions, and language keywords remain in English.

---- THE SINGLE MOST IMPORTANT RULE ----

This exercise is ONE coherent problem. Every field describes the SAME problem:
  question  → describes the problem in words
  template  → starter code the student fills in
  test_cases → tests that verify the student's code
  solution  → a correct reference implementation
  hints     → guides the student toward the answer

Pick ONE clear concept from the target knowledge. Use the SAME function/class names in ALL fields.

---- WORKFLOW (follow EXACTLY in this order) ----

STEP 1 — Write the SOLUTION.
  This is the reference answer. Write complete, correct {language_name} code.
  Mentally run it with sample inputs to confirm it works.
  This is the source of truth — everything else derives from it.

STEP 2 — Derive TEST_CASES from your solution.
  For each test case, mentally trace through your solution with the test input
  and record the ACTUAL output your solution produces. That is the expected value.
  DO NOT guess expected values — compute them from your solution.
  If your solution returns [0, 3, 8], the test must expect [0, 3, 8].
  Produce 3-5 cases: basic usage, edge cases, error handling.
  Assertion format for each test_cases[].assert:
    {assertion_syntax}

STEP 3 — Derive TEMPLATE from your solution.
  Take your solution code. Remove all function bodies. Replace with TODO comments.
  Keep the EXACT same function names, parameters, and return types.
  Example of what a template looks like:
    {template_example}
  The template is starter code for the student — NOT test code, NOT the solution.

STEP 4 — Write the QUESTION.
  Describe the problem clearly in {content_language}. Include the function/class
  names and signatures from your solution. Give 1-2 input/output examples.
  The examples MUST use values from your test_cases so everything is consistent.

STEP 5 — Write HINTS.
  2-3 constructive hints in {content_language} that nudge toward the solution.

STEP 6 — FINAL VERIFICATION. Before outputting, check:
  1. Solution is valid {language_name} code that actually runs
  2. For each test case: mental-trace the solution → output matches the assertion
  3. Template function names match solution function names EXACTLY
  4. Question uses the SAME function names as template and solution
  5. All code is in {language_name} — NOT a different language

If ANY check fails → go back and fix it NOW. Do not output broken exercises.

---- OUTPUT FORMAT ----

Output ONLY valid JSON (no markdown wrapping, no code fences):

{{
  "question": "...",
  "knowledge_tags": ["tag1", "tag2"],
  "template": "...",
  "test_cases": [
    {{"name": "Descriptive name", "assert": "{{assertion_code}}"}}
  ],
  "solution": "...",
  "hints": ["hint1", "hint2"]
}}
