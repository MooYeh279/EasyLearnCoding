You are generating a hands-on coding exercise for a programming student.
The student has just completed the following section of study.

Language: {language_name}
Topic: {topic_title}
Section: {section_title}
Target knowledge: {knowledge_description}

{platform_info}
{language_note}
Content language: {content_language}

ALL user-facing text MUST be in {content_language}: question, test_inputs[].name, hints, knowledge_tags.
Code identifiers and language keywords remain in English.

---- THE SINGLE MOST IMPORTANT RULE ----

This exercise is ONE coherent problem. Every field describes the SAME problem.
Pick ONE clear concept from the target knowledge. Use the SAME function/class names in ALL fields.

---- WORKFLOW (follow EXACTLY in this order) ----

STEP 1 — Write the SOLUTION.
  Write the COMPLETE correct code in {language_name}. Include ALL type definitions,
  helper declarations, and necessary imports. The code must be correct and compilable/runnable.
  Do NOT include program entry points (main function, test harness, driver code).
  The student will implement this same code on their own.

STEP 2 — Design TEST_INPUTS.
  For each test case, provide:
    - name: a short descriptive name (in {content_language})
    - input: a SELF-CONTAINED function call expression, e.g. add(1, 2)

  CRITICAL CONSTRAINT — every input expression MUST evaluate to one of:
    - int (integer)
    - float (floating-point number)
    - string
    - bool (true/false)

  For complex types (struct, class, array, vector), use member access to reach
  a basic type: point.x, vec.size(), arr[0], person.name.

  Rules for "input":
    - MUST be a self-contained expression — no references to undeclared variables.
      All data must be embedded inline (e.g. [1,2,3] or (int[]){{1,2,3}}).

  Produce 3-5 cases: basic usage, edge cases.

STEP 3 — Write QUESTION and HINTS.
  Question: describe the problem in {content_language}. Include:
  - What function(s)/class(es) the student needs to implement
  - The function signatures (names, parameters, return types)
  - 1-2 examples showing input and expected output
  Hints: 2-3 constructive hints in {content_language} to guide the student.

---- FINAL VERIFICATION ----
1. Solution is valid {language_name} code that actually runs
2. Every test_input expression is self-contained (no undeclared variables)
3. Every test_input expression evaluates to int, float, string, or bool
4. All code is in {language_name} — NOT a different language
5. Function/class names in question match those in solution

If ANY check fails → go back and fix it NOW.

---- OUTPUT FORMAT ----

Output ONLY valid JSON (no markdown wrapping, no code fences):

{{
  "question": "...",
  "solution": "...",
  "test_inputs": [
    {{"name": "Descriptive name", "input": "functionName(arg1, arg2)"}}
  ],
  "knowledge_tags": ["tag1", "tag2"],
  "hints": ["hint1", "hint2"]
}}
